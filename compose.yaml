  # docker-compose.yml
  services:
    db:
      image: postgres
      ports:
        - "5432:5432"
      environment:
        POSTGRES_PASSWORD: postgres
        POSTGRES_USERNAME: postgres
    app:
      build: .
      environment:
          SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/carbigdata


